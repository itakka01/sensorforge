# SensorForge v49 canonical configuration
# Cleaned from the supplied configuration on 2026-09-24.
# All 69 current canonical v49 configuration keys are present exactly once.
# Legacy keys were migrated to their current equivalents.
#
# SECURITY NOTE:
# wifi_pass, hotspot_password and web_password may be supplied as plaintext
# for provisioning. SensorForge protects persistent secret values with SFSEC1
# when the board-bound hardware key is available.
#
# This file preserves the effective settings of the supplied configuration.
# It is NOT a factory-default configuration.

############################################################
# AUTHENTICATION
############################################################

# 1 = automatically pause new recording automation while WebConfig is actively used; 0 = do not auto-pause
web_recording_auto_pause=1

# Web interface language: de or en
web_language=de

# 1 = HTTP authentication enabled; 0 = disabled
web_auth_enabled=1

# Web interface username
web_username=ja1

# Web interface password, 8..63 characters when authentication is enabled
web_password=sensor4337

############################################################
# CAMERA
############################################################

# Camera model: OV2640, OV3660 or OV5640
camera=OV3660

# Capture resolution
resolution=1024x768

# User-facing rotation: 0 or 180
rotation=0

# Frames per second
fps=5

# JPEG quality: 0 = best, 63 = lowest
quality=12

# Camera XCLK in MHz. v49 board defaults: Freenove=10, XIAO=20
camera_xclk_mhz=10

# 1 = automatic exposure enabled; 0 = disabled
camera_auto_exposure=1

# Automatic exposure target, -2..2
camera_ae_level=-2

# OV3660 crop zoom: 1.0, 1.5 or 2.0
camera_crop_zoom=1.0

# Crop position X: 0=left, 1=center, 2=right
camera_crop_x=1

# Crop position Y: 0=top, 1=center, 2=bottom
camera_crop_y=1

############################################################
# RECORDING / AUDIO / TRIGGER
############################################################

# Recording container: avi or mkv
recording_format=avi

# 1 = timestamp subtitles enabled; 0 = disabled
timestamp_enabled=1

# 1 = new recording media uses SensorForge encryption; 0 = normal storage
recording_encryption=0

# v49 audio foundation: 1 = audio capture enabled; 0 = disabled
audio_enabled=0

# Audio sample rate in Hz
audio_sample_rate=16000

# Audio sample depth
audio_bits_per_sample=16

# Audio channels
audio_channels=1

# Continuous shooter: 1 = enabled; 0 = disabled
shooter_enabled=1

# Continuous shooter storage format; canonical v49 value: mkv
shooter_storage_format=mkv

# Shooter sample interval in milliseconds
shooter_interval_ms=500

# Darkness threshold; 0 disables dark filter
shooter_dark_mean_min=20

# Minimum changed-area percentage; 0 disables similarity/change filter
shooter_min_change_pct=1.0

# Force-save interval; 0 disables forced keep
shooter_force_save_seconds=60

# PSRAM shooter flush interval; 0 = direct write
shooter_flush_seconds=300

# Maximum duration of one video segment; 0 = unlimited
recording_segment_seconds=30

# Maximum size of one video segment; 0 = unlimited
recording_segment_max_mb=20

# Maximum complete motion-event duration; 0 = unlimited
recording_event_max_seconds=65

# Cooldown after event safety limit; 0 = none
recording_event_cooldown_seconds=120

# Automatic recording gate: off or YYYY-MM-DDTHH:MM:SS
recording_not_before=off

# 1 = motion-triggered recording enabled; 0 = disabled
motion_recording_enabled=1

# direct, image_verify or image_only
motion_recording_decision=direct

# Image-motion sensitivity
image_motion_sensitivity=5

# Minimum image-motion area percentage
image_motion_min_area_pct=6

# Frames required to confirm image motion
image_motion_confirm_frames=2

# Frames required to release image motion
image_motion_release_frames=2

# Image-motion background learning setting
image_motion_background_learning=4

# Global mean-delta guard
image_motion_global_mean_delta=24

# Global-change guard percentage
image_motion_global_change_pct=70

# 20x15 image-motion ROI mask, 76 hex characters
image_motion_roi_mask=ffffffffffffffffffffffffffffffffffffffffffffffffffffffffffffffffffffffffff0f

# Post-recording time after last motion in milliseconds
post_record_ms=3000

############################################################
# SLEEP / POWER
############################################################

# off, light_sleep or deep_sleep
sleep_mode=light_sleep

# Idle delay before sleep in milliseconds
sleep_delay_ms=2000

# 1 = bootloop/undervoltage protection enabled; 0 = disabled
bootloop_protection=1

############################################################
# TRANSPORT MODE
############################################################

# 0 = normal operation; 1 = transport mode active
transport_mode=0

# Covered-camera check interval
transport_check_seconds=120

# Required bright confirmation time
transport_light_confirm_seconds=10

# Installation delay after cover removal
transport_install_delay_seconds=300

# Hard maximum transport duration
transport_max_duration_seconds=86400

# Canonical black threshold; P95 guard is derived internally as threshold + 10
transport_black_threshold=25

############################################################
# LED
############################################################

# 1 = recording/status LED enabled where available; 0 = disabled
led_enabled=1

############################################################
# STORAGE / SD SAFETY
############################################################

# Minimum free-space reserve on SD in MB
min_free_space_mb=100

# rollover or stop
disk_full_action=rollover

############################################################
# WIFI / HOTSPOT / TIME
############################################################

# off, on or on_missing_time
wifi_on_system_start=on

# Automatic Wi-Fi shutdown after inactivity; 0 = never auto-shut down
wifi_timeout_sec=60

# Device hostname; also used as hotspot SSID
hostname=printer51

# Infrastructure Wi-Fi SSID
wifi_ssid=wlan-ny

# Infrastructure Wi-Fi password
wifi_pass=klammer123

# 1 = hotspot enabled at startup; 0 = disabled
hotspot_enabled=1

# Hotspot password: empty = open AP, otherwise 8..63 characters
hotspot_password=sensor4337

# 1 = hidden hotspot SSID; 0 = visible
hotspot_hidden=0

# POSIX timezone string
timezone=CET-1CEST,M3.5.0,M10.5.0/3

############################################################
# DEBUG / LOGGING
############################################################

# 1 = debug enabled; 0 = disabled
debug_enabled=0

# Log file path
log_file=/log.txt

